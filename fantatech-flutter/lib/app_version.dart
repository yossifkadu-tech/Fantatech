// Single source of truth for the version shown in the UI (Profile version
// chip + Settings → About). Keep in sync with pubspec.yaml's `version:` —
// that one drives the actual Android/iOS build number, this one is just the
// display string.
const String kAppVersion = '1.00.00';
